{
  "schemaVersion": 1,
  "name": "jamesdadams-coucou",
  "version": "1.0.0",
  "displayName": "coucou",
  "description": "coucou2",
  "author": "JamesDAdams",
  "tags": [
    "community-pack"
  ],
  "contents": {
    "workflows": [
      {
        "id": "review",
        "name": "PR Review",
        "description": "PR Review + Merge workflow for OpenFox",
        "version": "1.1.0",
        "color": "#10b981",
        "entryStep": "setup",
        "steps": [
          {
            "id": "setup",
            "name": "Setup Workspace",
            "type": "agent",
            "phase": "build",
            "agentId": "builder",
            "prompt": "Set up the review workspace for PR #{{pr_number}}:\n\n1. Use the `workspace` tool to switch to a new workspace named `review-pr-{{pr_number}}`\n2. Point origin to GitHub: `git remote set-url origin git@github.com:co-l/openfox.git`\n3. Fetch and sync with develop: `git fetch origin && git reset --hard origin/develop`\n4. Checkout the PR branch: `gh pr checkout {{pr_number}}`\n\nIf `gh pr checkout` fails because the PR head branch name collides with a local branch (e.g. the PR head is also named `develop`), fetch the PR head ref directly instead:\n`git fetch origin refs/pull/{{pr_number}}/head && git checkout -b pr-{{pr_number}}-head FETCH_HEAD`\n\nIf any step fails, report the error and call step_done(). Otherwise call step_done().",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "check_base"
              }
            ]
          },
          {
            "id": "check_base",
            "name": "Verify Base Branch",
            "type": "agent",
            "phase": "build",
            "agentId": "builder",
            "prompt": "Verify the PR #{{pr_number}} targets develop:\n\n1. Check the base branch: `PR_BASE=$(gh pr view {{pr_number}} --json baseRefName --jq '.baseRefName')`\n2. If it's not \"develop\", retarget it: `gh api repos/co-l/openfox/pulls/{{pr_number}} -X PATCH -f base=develop && git fetch origin`\n3. Rebase onto latest develop: `git rebase origin/develop`\n\nIf rebase has conflicts, abort with `git rebase --abort` and report them. Otherwise call step_done().",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "review_report"
              }
            ]
          },
          {
            "id": "review_report",
            "name": "Review & Report",
            "type": "agent",
            "phase": "verification",
            "agentId": "builder",
            "prompt": "Review PR #{{pr_number}} and present findings:\n\n1. Read the diff: `git diff origin/develop...HEAD`\n2. List changed files: `git diff --stat origin/develop...HEAD`\n3. Run: `npm run test && npm run typecheck && npm run lint`\n4. Inspect code quality, error handling, edge cases\n\nPresent a clear summary to the user:\n- What was reviewed\n- Key findings with specific line references\n- Whether any fixes are needed (be specific about what and why)\n- If no fixes needed, state that clearly\n\nCall step_done() when done.",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "approve_fixes"
              }
            ]
          },
          {
            "id": "approve_fixes",
            "name": "Approve Fix Plan",
            "type": "user",
            "phase": "verification",
            "transitions": [
              {
                "when": {
                  "type": "step_result",
                  "result": "apply"
                },
                "goto": "apply_fixes"
              },
              {
                "when": {
                  "type": "step_result",
                  "result": "skip"
                },
                "goto": "start_dev_server"
              },
              {
                "when": {
                  "type": "always"
                },
                "goto": "apply_fixes"
              }
            ]
          },
          {
            "id": "apply_fixes",
            "name": "Apply Fixes",
            "type": "agent",
            "phase": "build",
            "agentId": "builder",
            "prompt": "Apply the fixes you identified during review:\n\n1. Implement the agreed fixes and improvements.\n2. Verify with: `npm run typecheck && npm run lint && npm run test`\n3. Leave everything UNCOMMITTED. Do NOT commit, tag, or push anything — the single review commit happens after manual testing.\n\nIf there is nothing to fix or apply, say so explicitly and call step_done().",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "start_dev_server"
              }
            ]
          },
          {
            "id": "start_dev_server",
            "name": "Start Dev Server",
            "type": "agent",
            "phase": "build",
            "agentId": "builder",
            "prompt": "Start the dev server using the `dev_server` tool so the user can manually test the changes. Once it's running, present a handoff summary:\n\n> **PR #{{pr_number}} is ready at http://localhost:<port>.**\n>\n> **What I fixed:**\n> - _bullet list of specific changes_\n> - _why each matters_\n> - _if no fixes were needed, say so explicitly instead of an empty list_\n>\n> **What to test:**\n> - _specific things to try from the user's perspective_\n> - _edge cases_\n\nDo NOT ask the user if they want the dev server started — just start it and present the summary. Call step_done() when done.",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "user_test"
              }
            ]
          },
          {
            "id": "user_test",
            "name": "User Testing",
            "type": "user",
            "phase": "verification",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "capture_fixes"
              }
            ]
          },
          {
            "id": "capture_fixes",
            "name": "Commit All Fixes & Adjustments",
            "type": "agent",
            "phase": "build",
            "agentId": "builder",
            "prompt": "All fixes and testing adjustments are now in the review workspace. Capture them in ONE commit:\n\n1. `git add -A`\n2. If there are staged changes: `git commit -m \"review: fixes for PR #{{pr_number}}\"`\n   If nothing is staged, report that there is nothing to commit.\n3. Tag the tip so it can be located later: `git tag -f review-fixes-{{pr_number}}`\n4. Push the tag: `git push origin review-fixes-{{pr_number}}`\n5. Report whether fixes were captured (yes/no) and call step_done().",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "prepare_merge"
              }
            ]
          },
          {
            "id": "prepare_merge",
            "name": "Prepare PR for Merge",
            "type": "agent",
            "phase": "verification",
            "agentId": "builder",
            "prompt": "Make sure PR #{{pr_number}} is mergeable before attempting the squash-merge:\n\n1. Check mergeability and PR head details:\n   `gh pr view {{pr_number}} --json mergeable,mergeStateStatus,headRepositoryOwner,headRefName,maintainerCanModify`\n2. If `mergeable` is \"MERGEABLE\": report it and call step_done() — the squash-merge step can proceed.\n3. If the PR is conflicting or otherwise not mergeable, rebuild the PR head on latest develop so it merges cleanly:\n   a. `git fetch origin && git fetch origin refs/pull/{{pr_number}}/head`\n   b. Create a fresh branch from the latest develop: `git checkout -b pr-{{pr_number}}-merged origin/develop`\n   c. Bring in the PR content plus all local review fixes (the `review-fixes-{{pr_number}}` tag from the capture step): `git merge --squash review-fixes-{{pr_number}}`\n   d. Resolve every conflict — when both sides add features, keep BOTH; never delete the other side's code.\n   e. Verify: `npm run typecheck && npm run lint && npm run test`, then `git add -A && git commit -m \"PR #{{pr_number}}: merged onto develop\"`.\n   f. Push the resolved head to the PR's head ref (only possible when `maintainerCanModify` is true):\n      `git push --force-with-lease=<headRefName>:<expectedSha> https://github.com/<headOwner>/<headRepo>.git pr-{{pr_number}}-merged:<headRefName>`\n   g. Re-check mergeability with step 1.\n4. If the PR still is not mergeable, or pushing to the fork's head is not possible, report the blocker explicitly so the user can intervene, then call step_done().",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "squash_merge"
              }
            ]
          },
          {
            "id": "squash_merge",
            "name": "Squash-Merge PR",
            "type": "agent",
            "phase": "build",
            "agentId": "builder",
            "prompt": "Squash-merge PR #{{pr_number}} via API:\n\n1. `gh api repos/co-l/openfox/pulls/{{pr_number}}/merge -X PUT -f merge_method=squash -f commit_title=\"PR #{{pr_number}}: $(gh pr view {{pr_number}} --json title --jq '.title')\"`\n2. Call step_done().",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "cherry_pick"
              }
            ]
          },
          {
            "id": "cherry_pick",
            "name": "Confirm Merge on Develop",
            "type": "agent",
            "phase": "verification",
            "agentId": "builder",
            "prompt": "Switch to original and confirm the merge landed on develop:\n\n1. `workspace switch original`\n2. `git checkout develop && git pull origin develop --ff-only`\n3. Confirm the squash-merge commit exists: `git log --oneline -3 develop` (look for `PR #{{pr_number}}: ...`).\n4. The review fixes were folded into that single squash commit (one-commit design), so no separate cherry-pick is needed. If the squash commit is missing, report it loudly.\n5. Call step_done().",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "verify_push"
              }
            ]
          },
          {
            "id": "verify_push",
            "name": "Verify Fixes on Origin",
            "type": "agent",
            "phase": "verification",
            "agentId": "builder",
            "prompt": "Verify the PR's changes are present in git history on both local and origin develop, before deleting the workspace:\n\n1. `git fetch origin` then confirm local and remote are in sync: `git rev-parse develop origin/develop` (SHAs must match)\n2. Confirm the PR's squash-merge commit (`PR #{{pr_number}}: ...`) is in history on both: `git log --oneline -5 develop` and `git log --oneline -5 origin/develop`\n3. If review fixes were applied: they are folded into the single squash-merge commit (one-commit design) — there is no separate `review: fixes for PR #{{pr_number}}` commit on develop. State that and confirm the fix content is present by spot-checking one of the fixed files: `git show origin/develop:<file>`\n4. Spot-check the merged changes exist on origin: pick a file the PR touched and confirm via `git show origin/develop:<file>`\n\nReport whether the changes are confirmed on both local and origin, then call step_done().",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "cleanup"
              }
            ]
          },
          {
            "id": "cleanup",
            "name": "Clean Up",
            "type": "agent",
            "phase": "build",
            "agentId": "builder",
            "prompt": "Remove temporary artifacts and delete the workspace:\n\n1. Remove remote tag if it exists: `git push origin --delete refs/tags/review-fixes-{{pr_number}} 2>/dev/null; true`\n2. Remove local tag if it exists: `git tag -d review-fixes-{{pr_number}} 2>/dev/null; true`\n3. `workspace delete review-pr-{{pr_number}}`\n4. Call step_done().",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "pr_feedback"
              }
            ]
          },
          {
            "id": "pr_feedback",
            "name": "Leave Feedback on PR",
            "type": "agent",
            "phase": "verification",
            "agentId": "builder",
            "prompt": "Leave closing feedback on PR #{{pr_number}} once the user has approved it:\n\n1. Determine the next release version: read the current version from package.json and use the next patch bump (e.g. 2.0.112 -> v2.0.113). If you can't determine it confidently, ask the user.\n2. Draft the PR comment with EXACTLY this format (the `### AI-summary` heading is mandatory, no AI-written intro paragraph). The AI-summary bullets describe what WE, the maintainers, did on this PR during the review — rebase, conflict resolution, fixes applied, verification — NOT the PR's own feature content:\n\nMerged, will be released in vX.Y.Z\n---\n### AI-summary\n- <bullet point>\n- <bullet point>\n\nExample:\n\nMerged, will be released in v2.0.132\n---\n### AI-summary\n- Rebased the PR onto latest develop and resolved merge conflicts with the LLM capability tables (kept both branches' capabilities)\n- Fixed the affected tests to match the merged types and verified the whole suite (unit + e2e green, typecheck and lint clean)\n- Pushed the rebased branch to the contributor's fork so the PR merges cleanly with full attribution\n\n3. Present the full draft in chat and ask the user to approve or modify it.\n4. Do NOT post the comment until the user explicitly approves the final text.\n5. Once approved, post it: write the text to a temp file and run `gh pr comment {{pr_number}} --body-file <tempfile>`, then remove the temp file.\n\nCall step_done() when you have presented the draft.",
            "transitions": [
              {
                "when": {
                  "type": "always"
                },
                "goto": "$done"
              }
            ]
          }
        ],
        "settings": {
          "maxIterations": 20
        }
      }
    ],
    "skills": [
      {
        "id": "handoff",
        "name": "handoff",
        "description": "Compact the current conversation into a handoff document for another agent to pick up.",
        "prompt": "Write a handoff document summarising the current conversation so a fresh agent can continue the work. Save to the temporary directory of the user's OS - not the current workspace.\n\nInclude a \"suggested skills\" section in the document, naming which skills the next agent should call the Skill tool for.\n\nDo not duplicate content already captured in other artifacts (specs, plans, ADRs, issues, commits, diffs). Reference them by path or URL instead.\n\nRedact any sensitive information, such as API keys, passwords, or personally identifiable information.\n\nIf the user passed arguments, treat them as a description of what the next session will focus on and tailor the doc accordingly."
      }
    ]
  }
}